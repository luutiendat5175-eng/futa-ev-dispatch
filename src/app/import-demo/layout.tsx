import { AppShell } from '@/components/layout/AppShell';
import './import-data.css';

export default function ImportLayout({ children }: { children: React.ReactNode }) { return <AppShell>{children}</AppShell>; }
