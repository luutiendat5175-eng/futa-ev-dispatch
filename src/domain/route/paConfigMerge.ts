export const paTextKey=(value:string)=>value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[đĐðÐ]/g,'d').toLowerCase().replace(/[^a-z0-9]+/g,'');
export const paRowKey=(routeCode:string,routeEndKey:string)=>`${routeCode.trim()}|${paTextKey(routeEndKey.replace(/__(A|B)$/i,'__$1'))}`;
export type StoredPaRow={route_code:string;route_name:string;route_end_name:string;route_end_key:string;overnight_depot_id:string;charging_station_id:string;planned_vehicle_count:number;mobilization_minutes:number;buffer_minutes:number;note:string|null};
export const samePaValues=(a:StoredPaRow,b:StoredPaRow)=>a.route_name===b.route_name&&a.route_end_name===b.route_end_name&&a.overnight_depot_id===b.overnight_depot_id&&a.charging_station_id===b.charging_station_id&&Number(a.planned_vehicle_count)===Number(b.planned_vehicle_count)&&Number(a.mobilization_minutes)===Number(b.mobilization_minutes)&&Number(a.buffer_minutes)===Number(b.buffer_minutes)&&(a.note??'')===(b.note??'');
export function mergePaRows(current:StoredPaRow[],incoming:StoredPaRow[]){const merged=[...current],added:StoredPaRow[]=[],changed:Array<{before:StoredPaRow;after:StoredPaRow}>=[];for(const row of incoming){const key=paRowKey(row.route_code,row.route_end_key),index=merged.findIndex(item=>paRowKey(item.route_code,item.route_end_key)===key);if(index<0){merged.push(row);added.push(row)}else if(!samePaValues(merged[index],row)){changed.push({before:merged[index],after:row});merged[index]=row}}return{merged,added,changed}}
